<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.lec.spring.repository.AipageRepository">

    <select id="findCityByAnswers" parameterType="map" resultType="com.example.domain.City">
        SELECT *
        FROM city
        WHERE q1_id = #{q1Id}
          AND q2_id = #{q2Id}
          AND q3_id = #{q3Id}
          AND q4_id = #{q4Id}
          AND q5_id = #{q5Id};
    </select>

<!--    //도시별 정보 조회, 그런데 질문값을 빠트린-->
    <select id="findCityById" parameterType="map" resultType="com.example.domain.City">
        SELECT *
        FROM city
        WHERE q1_id = #{q1Id}
          AND q2_id = #{q2Id}
          AND q3_id = #{q3Id}
          AND q4_id = #{q4Id}
          AND q5_id = #{q5Id};
    </select>

</mapper>