<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.lec.spring.mytrip.repository.FeedRepository">

    <sql id="SELECT_BOARD">
        select
            board_id "boardId",
            user_id "userId",
            city_id "cityId",
            board_subject "boardSubject",
            board_content "boardContent",
            board_view_count "boardViewCount",
            board_date "boardDate",
            board_category "boardCategory"
        from board
        where 1=1
    </sql>

    <!-- 피드 저장 -->
    <insert id="save" parameterType="com.lec.spring.mytrip.domain.Feed" useGeneratedKeys="true" keyColumn="id" keyProperty="id">
        INSERT INTO board
            (user_id, city_id, board_subject, board_content, board_view_count, board_date, board_category)
        VALUES
            (#{userId}, #{cityId}, #{boardSubject}, #{boardContent}, #{boardViewCount}, #{boardDate}, #{boardCategory})
    </insert>

    <!-- 특정 피드 id로 글 찾기 -->
    <select id="findById" parameterType="long" resultType="com.lec.spring.mytrip.domain.Feed">
        <include refid="SELECT_BOARD"/>
        AND board_id = #{boardId}
    </select>

    <!-- 특정 피드 id 글 조회수 증가 -->
    <update id="viewCnt" parameterType="long">
        UPDATE board
        SET board_view_count = board_view_count + 1
        AND board_id = #{boardId}
    </update>

    <!-- 전체 글 목록 : 최신순 -->
    <select id="findAll" resultType="com.lec.spring.mytrip.domain.Feed">
        <include refid="SELECT_BOARD" />
        ORDER BY board_date DESC
    </select>

    <!-- 특정 유저 id의 피드 최신순 리스트  -->
    <select id="findByUserId" parameterType="long" resultType="com.lec.spring.mytrip.domain.Feed">
        <include refid="SELECT_BOARD" />
        AND user_id = #{userId}
        ORDER BY board_date DESC
    </select>

    <!-- 특정 사용자의 최신 게시물 9개 가져오기 -->
    <select id="findRecentFeedByUser" parameterType="long" resultType="com.lec.spring.mytrip.domain.Feed">
        <include refid="SELECT_BOARD"/>
        AND user_id = #{userId}
        ORDER BY board_date DESC
        LIMIT 9
    </select>

    <!-- 특정 피드 id로 피드 수정 -->
    <update id="update" flushCache="true" parameterType="com.lec.spring.mytrip.domain.Feed">
        UPDATE board
        SET board_subject = #{boardSubject},
            board_content = #{boardContent},
            board_category = #{boardCategory},
            city_id = #{cityId}
        WHERE board_id = #{boardId}
    </update>

    <!-- 특정 피드 id로 피드 삭제  -->
    <delete id="delete" parameterType="long">
        DELETE FROM board
        WHERE board_id = #{boardId}
    </delete>

    <!-- 전체 피드 개수 -->
    <select id="feedCountAll" resultType="int">
        SELECT COUNT(*) FROM board
    </select>

    <!-- 첨부파일 -->
    <insert id="saveAttachment" parameterType="com.lec.spring.mytrip.domain.PostAttachment">
        INSERT INTO board_attachment (
            board_id,
            board_attachment_file
        ) VALUES (
                     #{boardId},
                     #{boardAttachmentFile}
                 )
    </insert>

    <select id="findAttachmentByBoardId" resultType="com.lec.spring.mytrip.domain.PostAttachment">
        SELECT board_attachment_id, board_id, board_attachment_file
        FROM board_attachment
        WHERE board_id = #{boardId}
    </select>

    <delete id="deleteAttachment" parameterType="long">
        DELETE FROM board_attachment
               WHERE board_attachment_id = #{boardAttachmentId}
    </delete>

    <delete id="deleteAttachmentByBoardId" parameterType="long">
        DELETE FROM board_attachment WHERE board_id = #{boardId}
    </delete>

    <select id="findAllCities" resultType="com.lec.spring.mytrip.domain.City">
        SELECT * FROM city
    </select>

    <select id="findCityById" resultType="com.lec.spring.mytrip.domain.City">
        SELECT * FROM city WHERE city_id = #{cityId}
    </select>

</mapper>