<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.lec.spring.mytrip.domain.User">
    <sql id="SELECT_USER">
        select
            user_id "id",
            user_email "email",
            user_password "password",
            user_username "username",
            user_phonenumber "phonenumber",
            user_regdate "regdate",
            user_profile "profile",
            user_provider "provider",
            user_introdution "introdution",
            user_authorization "authorization",
            business_companynumber "companynumber"
        from user
        where 1=1
    </sql>
    <select id="findById" resultType="com.lec.spring.mytrip.domain.User">
        <include refid="SELECT_USER"/>
        and user_id = #{user_id}
    </select>

    <select id="findBuUsername" resultType="com.lec.spring.mytrip.domain.User">
        <include refid="SELECT_USER"/>
        and user_username = #{user_username}
    </select>

    <insert id="save" flushCache="true" parameterType="com.lec.spring.mytrip.domain.User"
    useGeneratedKeys="true" keyProperty="user_id" keyColumn="user_id">
        insert into user (user_email, user_password, user_username, user_name, user_phonenumber, user_birthday, user_provider, business_companynumber)
        values(#{user_email}, #{user_password}, #{user_usernmame}, #{user_name} , #{user_phonenumber}, #{user_birthday}, #{user_provider}, #{business_companynumber})
    </insert>

<!--    <update></update>-->
</mapper>

















